/**
 * \file    min_swap_sort.h
 * \brief
 * \details
 * \author  conquistador
 */
#ifndef MIN_SWAP_SORT_MIN_SWAP_SORT_H_
#define MIN_SWAP_SORT_MIN_SWAP_SORT_H_

#include <vector>

/**
 * \brief Minimal quantity to sort sequence in order:
 *        a[0] > a[1] > a[2] > … > a[k - 1] > a[k] < a[k+1] < a[k+2] < … a[N]
 */
int n_swaps(const std :: vector<int> &a);

#endif /* MIN_SWAP_SORT_MIN_SWAP_SORT_H_ */
